# Vault Configuration
VAULT_ADDR=http://vault:8200
VAULT_TOKEN_PATH=/vault-keys/vault-keys.json

# PostgreSQL Configuration
POSTGRES_USER=archiver
POSTGRES_DB=documents_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# MinIO Configuration
MINIO_ENDPOINT=minio:9000
MINIO_BUCKET=documents

# NGINX
NGINX_PORT=80
